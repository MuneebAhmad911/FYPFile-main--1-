import React from 'react'
import { useLocation } from 'react-router-dom';

function EscrowContractDetails() {
      const location = useLocation();
      const { item } = location.state;
  return (
    <div>
      
    </div>
  )
}

export default EscrowContractDetails
